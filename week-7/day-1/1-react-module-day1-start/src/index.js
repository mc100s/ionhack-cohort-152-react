import React from "react";
import ReactDOM from "react-dom";
import App from "./App.js";

// Inject the App component into the tag with the id "root"
ReactDOM.render(
  <App />,
  document.getElementById("root")
);